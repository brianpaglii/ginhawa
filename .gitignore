# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# Coverage artifacts (only the markdown snapshots in
# docs/verification/ are the durable record)
.coverage
.coverage.*
coverage.json
htmlcov/

# Node
node_modules/
dist/
.vite/

# Environment files
.env
.env.*
!.env.example
*.pem
*.key

# Bench-only MQTT credential file used by
# scripts/bench_mqtt_publish.sh; the *.example template is committed.
scripts/.bench_mqtt_secrets

# Firmware secrets (Wi-Fi PSK + Mosquitto password baked into each
# ESP32 image) — secrets.h.example is committed; secrets.h is not.
firmware/*/include/secrets.h

# Local databases
*.db
*.sqlite
*.sqlite3

# OS files
.DS_Store
Thumbs.db

# PlatformIO
.pio/

# Claude Code
.claude/
